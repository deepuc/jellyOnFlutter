name: authentication_repository
description: A repository that handles auth related requests
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  authentication_api:
    path: ../authentication_api
  dio: ^5.4.3+1
  drift: ^2.17.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  shared_preferences: ^2.2.3
  sqlite_database:
    path: ../sqlite_database

dev_dependencies:
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  mocktail: ^1.0.3
  pedantic: ^1.11.1
